import PageStub from '@/components/PageStub';

export default function QaAuditDashboard() {
  return (
    <PageStub
      title="QA & Audit Dashboard"
      batch={8}
      summary="Approval rate, ERS drift, eval-score trends, dead-link counts, DLQ depths, cost dashboard."
    />
  );
}
