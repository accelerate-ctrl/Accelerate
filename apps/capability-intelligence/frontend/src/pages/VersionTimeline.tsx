import PageStub from '@/components/PageStub';

export default function VersionTimeline() {
  return (
    <PageStub
      title="Version Timeline"
      batch={1}
      summary="Every catalogue snapshot with provenance, author, summary, and replay link."
    />
  );
}
