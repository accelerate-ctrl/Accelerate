import PageStub from '@/components/PageStub';

export default function WhatIfSimulator() {
  return (
    <PageStub
      title="What-If Simulator"
      batch={8}
      summary="Sandboxed counterfactual edits with downstream impact preview (orphans, remappings, benchmark deltas)."
    />
  );
}
