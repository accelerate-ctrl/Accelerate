import PageStub from '@/components/PageStub';

export default function AiChat() {
  return (
    <PageStub
      title="AI Chat"
      batch={8}
      summary="RAG chat over catalogue + KG; citations inline; cost meter visible."
    />
  );
}
