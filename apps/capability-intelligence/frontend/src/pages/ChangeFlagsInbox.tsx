import PageStub from '@/components/PageStub';

export default function ChangeFlagsInbox() {
  return (
    <PageStub
      title="Change Flags Inbox"
      batch={1}
      summary="Open mapping regressions, theme alignment violations, drift alerts; assign + resolve workflow."
    />
  );
}
