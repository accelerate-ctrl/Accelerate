import PageStub from '@/components/PageStub';

export default function ProjectSubcapTrace() {
  return (
    <PageStub
      title="Project–Subcap Trace"
      batch={3}
      summary="Pick a subcap → all SOWs and stories that touched it, with mention timeline and project metadata."
    />
  );
}
