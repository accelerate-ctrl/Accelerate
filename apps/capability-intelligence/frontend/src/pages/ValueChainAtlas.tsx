import PageStub from '@/components/PageStub';

export default function ValueChainAtlas() {
  return (
    <PageStub
      title="Value Chain Atlas"
      batch={2}
      summary="8 universal MECE clusters (VCC-01 → VCC-08), per-subvertical stage mapping, customer-outcome arrows."
    />
  );
}
