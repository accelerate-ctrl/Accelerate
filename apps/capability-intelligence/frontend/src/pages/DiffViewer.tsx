import PageStub from '@/components/PageStub';

export default function DiffViewer() {
  return (
    <PageStub
      title="Diff Viewer"
      batch={1}
      summary="Field-level diff between any two versions with claim-labeled narrative explanation."
    />
  );
}
