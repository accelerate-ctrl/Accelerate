import PageStub from '@/components/PageStub';

export default function PlatformCatalog() {
  return (
    <PageStub
      title="Platform Catalog"
      batch={2}
      summary="All ~45 L3 platforms + ~200 L4 features grouped by vendor; cross-link to subcaps using each."
    />
  );
}
