import PageStub from '@/components/PageStub';

export default function ClientJourneyAtlas() {
  return (
    <PageStub
      title="Client Journey Atlas"
      batch={6}
      summary="Per-client timeline (9 event types) and DMA App handoff JSON export."
    />
  );
}
