import PageStub from '@/components/PageStub';

export default function MissionControl() {
  return (
    <PageStub
      title="Mission Control"
      batch={1}
      summary="Cross-pillar tiles, source-health widget, persona switcher, recent activity, top open flags & suggestions."
    />
  );
}
