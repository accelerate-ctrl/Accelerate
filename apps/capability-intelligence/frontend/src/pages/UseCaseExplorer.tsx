import PageStub from '@/components/PageStub';

export default function UseCaseExplorer() {
  return (
    <PageStub
      title="Use Case Explorer"
      batch={2}
      summary="~3000 use cases tagged across 22 UC tags (5 families); drill from tag → subcap → story."
    />
  );
}
