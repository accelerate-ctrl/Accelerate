import PageStub from '@/components/PageStub';

export default function VendorIntelligence() {
  return (
    <PageStub
      title="Vendor Intelligence"
      batch={6}
      summary="Vendor × subcap heatmap; events; win/loss; client adoptions and replacements."
    />
  );
}
