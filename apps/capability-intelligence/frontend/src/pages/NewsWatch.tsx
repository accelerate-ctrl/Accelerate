import PageStub from '@/components/PageStub';

export default function NewsWatch() {
  return (
    <PageStub
      title="News Watch"
      batch={4}
      summary="Canonical-source news feed (50+ sources, T1–T3); fs_relevance scoring; click-through to subcap mapping."
    />
  );
}
