import PageStub from '@/components/PageStub';

export default function CapabilityExplorer() {
  return (
    <PageStub
      title="Capability Explorer"
      batch={1}
      summary="Sunburst + tree + search across the 4 pillars / 17 categories / ~144 L1 / ~836 subcaps with multi-lens overlays."
    />
  );
}
