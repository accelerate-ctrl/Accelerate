import PageStub from '@/components/PageStub';

export default function StoryLibrary() {
  return (
    <PageStub
      title="Story Library"
      batch={3}
      summary="Live Jira stories side-by-side with the canonical gen-stories file; quality + persona scoring."
    />
  );
}
